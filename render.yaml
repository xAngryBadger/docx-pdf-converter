services:
  - type: web
    name: capivara-api
    runtime: python
    build:
      commands:
        - pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
    plan: free
